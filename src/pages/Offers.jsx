// ═══════════════════════════════════════════════════════════════════════
// Offers — RETIRED (May 2026)
// ───────────────────────────────────────────────────────────────────────
// An employment offer is no longer a standalone entity or page. "Offer" is
// simply a stage in the candidate hiring pipeline:
//     Applied → Screening → Interview → Offer → Hired
// Moving a candidate to the "Offer" stage is a plain, audit-logged stage
// change (an optional note is captured for the audit trail). Moving a
// candidate to "Hired" auto-spawns their Onboarding application.
//
// This file is intentionally left as an empty module — the old /offers
// route now redirects to the Recruitment Pipeline (see App.jsx). Nothing
// imports this file; it is kept only because the build environment does
// not permit deleting source files.
// ═══════════════════════════════════════════════════════════════════════

export {};
