# EREP UX Spec — Developer Directory + Detail

## Snapshot
- URL of directory: not implemented in EREP. No `src/app/[locale]/find`, corporate directory route, or developer/broker page exists in the routed app; navbar links only Buy, Sell, Mortgage, while footer links omit Find. File refs: `src/components/layout/navbar/index.tsx:26`, `src/components/layout/footer/index.tsx:11`, `src/app/[locale]/[...rest]/page.tsx:5`.
- URL of detail: not implemented in EREP. Corporate/developer detail endpoints exist, but no consumer route renders them. The only live consumer corporate surface is embedded in property detail at `/[locale]/buy/<sub-type-1>/<sub-type-2>/<MLS-ID>`. File refs: `src/app/[locale]/buy/[[...slugs]]/page.tsx:40`, `src/app/[locale]/buy/[[...slugs]]/page.tsx:316`, `src/app/[locale]/buy/[[...slugs]]/page.tsx:522`.
- Key components: API stubs and types exist for a future directory/detail/project experience, but are unused outside `src/apis.ts` and `src/types/find.ts`. Property detail uses corporate data in specs, inquiry sidebar, and MLS verified/listing-office card. File refs: `src/apis.ts:430`, `src/apis.ts:437`, `src/apis.ts:444`, `src/apis.ts:458`, `src/apis.ts:473`, `src/types/find.ts:1`, `src/app/[locale]/property/[id]/components/specsSection.tsx:59`, `src/app/[locale]/property/[id]/components/inquiryCard.tsx:19`, `src/app/[locale]/property/[id]/components/mlsVerifiedSection.tsx:96`.

## 1. Developer Directory
- **Layout**: not implemented in EREP. There is no consumer page chrome for directory browse: no mounted breadcrumb/title/count/search/filter/sort UI. The app route list is limited to home, buy, sell, mortgage, favorites, compare, profile, notifications, auth, FAQ, bank-form, verify-QR, policies, and catch-all not-found; no `find` route is mounted. File refs: `src/app/[locale]/[...rest]/page.tsx:5`, `src/components/layout/navbar/index.tsx:26`, `src/components/layout/footer/index.tsx:11`.
- **Card fields**: not implemented in EREP. The intended data shape is visible only in `DeveloperBroker`: `id`, `logo`, `name`, `mls_id`, `type`, `projects`, `units`, and `slug`. There is no rendered card with cover image, governorate chips, or portfolio CTA. File refs: `src/types/find.ts:1`, `src/apis.ts:437`.
- **Filters / sort**: not implemented in EREP. API helper `GET_DEVELOPERS_BROKERS({ params })` accepts an arbitrary params string and `GET_DEVELOPERS_BROKERS_COUNTS()` exists, but no UI builds params for search, type tabs, filters, sort, or pagination. Locale strings suggest a planned page with title `Developers & Brokerages`, search by name/MLS ID, developer/brokerage tabs, and no-results text, but no component consumes them. File refs: `src/apis.ts:430`, `src/apis.ts:437`, `src/locales/en.json:565`, `src/locales/en.json:567`, `src/locales/en.json:568`, `src/locales/en.json:570`.
- **Pagination / loading / empty**: not implemented in EREP. No directory component consumes `meta`, renders skeletons, empty state, infinite scroll, or per-page count. Only endpoint stubs exist. File refs: `src/apis.ts:437`, `src/types/find.ts:1`.
- **Mobile behavior**: not implemented in EREP.
- **File refs**: `src/apis.ts:430`, `src/apis.ts:437`, `src/types/find.ts:1`, `src/locales/en.json:565`, `src/components/layout/navbar/index.tsx:26`, `src/components/layout/footer/index.tsx:11`, `src/app/[locale]/[...rest]/page.tsx:5`.

## 2. Developer Detail
- **Hero**: not implemented in EREP. No routed corporate detail page exists, so there is no hero with cover image, logo, tagline, stats, save developer CTA, map, reviews, or similar developers. Endpoint `GET_CORPORATE_DETAIL({ corporate_id })` fetches `/corporates/<id>`, but no consumer component calls it. File refs: `src/apis.ts:473`.
- **Sections (in order)**: not implemented in EREP as a standalone developer detail page. Planned project-detail data types exist for project name, images, city, start price, description, key features, available units, facts/features, and slug, but no route renders them. File refs: `src/types/find.ts:91`, `src/apis.ts:484`, `src/apis.ts:500`.
- **Project list**: not implemented in EREP as a developer detail block. API helper `GET_DEVELOPER_PROJECTS` fetches `/projects?corporate_id=<id>...`, and `ApiProject` contains `id`, `slug`, `name`, `image`, `city_name`, `properties_count`, and `start_price`. Locale strings suggest project cards with “Projects by”, “Starting from”, and units labels, but there is no component rendering cards, sorting, filtering, or linking to project pages. File refs: `src/apis.ts:444`, `src/types/find.ts:43`, `src/locales/en.json:573`, `src/locales/en.json:574`, `src/locales/en.json:575`.
- **Available units / corporate properties**: not implemented as developer detail UX. API helper `GET_BROKER_PROPERTIES` fetches `/properties/by-corporate/<id>`, and `GET_AVILABLE_UNITS` fetches `/projects/avilable-units/<id>`, but no consumer page renders a corporate listings tab or project units table. File refs: `src/apis.ts:458`, `src/apis.ts:500`, `src/types/find.ts:53`, `src/types/find.ts:64`.
- **Contact / CTA**: no standalone developer contact CTA. On property detail only, corporate identity appears in the inquiry card as agent/company name with initials avatar, and inquiry submits against the property MLS ID rather than corporate ID. The MLS verified card shows listing office logo/name/MLS ID. No call, WhatsApp, schedule, or save-developer CTA is wired to the corporate itself. File refs: `src/app/[locale]/property/[id]/components/cardsSection.tsx:54`, `src/app/[locale]/property/[id]/components/inquiryCard.tsx:82`, `src/app/[locale]/property/[id]/components/inquiryCard.tsx:118`, `src/app/[locale]/property/[id]/components/mlsVerifiedSection.tsx:96`, `src/app/[locale]/property/[id]/components/mlsVerifiedSection.tsx:105`.
- **File refs**: `src/apis.ts:444`, `src/apis.ts:458`, `src/apis.ts:473`, `src/apis.ts:484`, `src/apis.ts:500`, `src/types/find.ts:43`, `src/types/find.ts:91`, `src/app/[locale]/property/[id]/components/specsSection.tsx:59`, `src/app/[locale]/property/[id]/components/mlsVerifiedSection.tsx:96`, `src/app/[locale]/property/[id]/components/inquiryCard.tsx:19`.

## Concrete fixes in Homes
- file: `src/pages/MarketplaceSite/Find.jsx` — EREP has no live directory to mirror exactly; build from its unused contract shape: tabs for Developers/Brokerages, search by name or MLS ID, counts, no-results state, and cards showing logo/initials, name, MLS ID, projects count, units count, and View Portfolio CTA.
- file: new `src/pages/MarketplaceSite/DeveloperDetail.jsx` — create the missing page EREP only implies: hero with logo/name/MLS ID/stats; sections for About, Projects, Available Units/Listings, and Contact. Keep CTAs property/corporate-scoped: View Projects, View Listings, Contact, WhatsApp/phone if Homes data has them.
- file: `src/data/publicMarketplaceData.js` or `src/data/marketplaceData.js` — extend `MP_DEVELOPERS` with `logo`, `mls_id`, `type`, `slug`, `units`, `coverImage`, `description`, `governorates`, `projectsList`, and optional contact fields.
- file: `src/components/MarketplaceSiteLayout.jsx` — link Find to the directory route; header counters/auth should not imply EREP has save-developer behavior because it is not implemented there.
